# Multi-agent PPO (shared encoder + per-agent heads + centralized critic)
rollout_steps: 2048
num_updates: 2000
hidden_dim: 128
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
learning_rate: 0.0003
entropy_coef: 0.01
value_coef: 0.5
max_grad_norm: 0.5
ppo_epochs: 4
num_minibatches: 4
log_interval: 20
